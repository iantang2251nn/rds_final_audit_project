# Suggested fixes — post-revision pass

**Status as of 2026-05-08 (after report.tex commits since `04ddb66`).** A substantial revision has landed: §5.4 promoted to a full subsection, §1 now opens with the 2018→2019 metric-design lineage and the "system × metric pairing" framing, §7 contributions now matches the partner-agreed division, intro scope-honesty fixed, original 3rd-place leaderboard score added (0.94683, gap ~0.002), §6.2 stakeholder verdict expanded with three explicit grounds + alternative-anchor reasoning, "Pillar 1/2/3" terminology removed, "Module 1" jargon removed, "Rawlsian aggregation" → "difference-principle aggregation" in §5.4 closing, derived-md paths corrected to `inference_outputs/`, headers retitled (§5.1 → "Discriminative parity", §6 → "Reflection"), §3.2 calibration-implication softened, threshold-sensitivity acknowledged in limitation (vi), perplexity-cutoff arbitrariness acknowledged in limitation (iii), CF-score metric defined in §5.3.

**Page count: 10/10 (held).** **Net new finding: one factual error introduced by the revision (item 1 below) — fixable in one word.**

| # | Severity | Location | Issue | Suggested fix |
|---|---|---|---|---|
| 1 | blocker | §5.3:141 | **Factual error introduced in revision.** Line reads: "twelve identity tokens (the nine evaluated subgroups plus `asian`, `heterosexual`, and `transgender`; full list in `cell039`)". Per `inference_outputs/cell039_markdown-templates-identities-run-inference-across-bert-lstm_out01.txt` (rows 0–11), the actual twelve are: 9 evaluated + `asian` + **`straight`** + `heterosexual` (NOT `transgender`). The report makes a falsifiable claim explicitly pointing the grader at cell039; opening that file shows the contradiction immediately. (Pass-1 of this review guessed `transgender`; pass-2 corrected to `straight` after verifying cell039; the revision picked up the pass-1 guess.) | One-word fix on §5.3:141: `transgender` → `straight`. Optionally add one clause noting `straight`/`heterosexual` are synonymous identity terms (cell039 shows their LSTM tox scores differ ~2.7× on the same template, which is a label-form sensitivity finding; mention or skip per page budget). |
| 2 | blocker | §6.2:194–196 + §5.2 Table 2 | **TPR collapse still not surfaced in the verdict.** Every per-identity TPR at $s_{HR}=0.5$ is below OVERALL 0.7363 (range [0.4048, 0.7037]; six of nine in 40–50%); the model misses 30–60% of toxic identity-mentioning comments at the audit's chosen threshold. **The 2019 metric is AUC-based — rank-only, threshold-invariant — so this absolute-recall depression is structurally invisible to the leaderboard score the system was selected against.** §6.2 verdict still cites only "FPR varies ~5×". Given the §1 framing now explicitly pairs system and metric analysis "as co-equal objects" and §5.4 closes with "the bias-aware score is operationally usable but normatively under-specified", omitting the canonical example of metric-blindness from the verdict undercuts the audit's own framing. | Add one or two sentences to the §6.2 verdict paragraph: "The system also depresses TPR uniformly across the 9 identities (range [0.40, 0.70] vs OVERALL 0.74; Table 2). Because the bias-aware metric is rank-based and threshold-invariant, this absolute-recall collapse is structurally invisible to the score the system was selected against — the canonical example of the §5.4 metric-blindness story." |
| 3 | major | §6.2:194–196 + §5.2 Table 2 (selection-rate paradox) | **"Differential silencing" framing is precise but unanchored to data.** Every per-identity sel_rate is below its base rate at $s_{HR}=0.5$ (christian 0.0512 vs base 0.0996; black 0.1840 vs 0.3233; even "over-flagged" psychiatric 0.2059 vs 0.2269). The §6.2 claim is technically defensible — "differential silencing" refers to FPR elevation among non-toxic identity mentions, not absolute selection rate — but a reader who looks at Table 2 and computes sel/base sees ratios in [0.51, 0.91] and may be confused. The threshold-invariance of AUC-based metrics is the same metric-coupling story as item 2. | One clause in §6.2 paragraph (a) or in the "differential silencing" sentence: "By 'silencing' we mean elevated FPR among non-toxic identity mentions; absolute selection rates fall below base rates for every evaluated identity at $s_{HR}=0.5$, a threshold-positioning artifact the AUC-based metric is invariant to." |
| 4 | major | §3.2:44, §5.1, §5.2, §5.3:141 (multi-head scope) | The model has 7 prediction heads (toxicity + 6 subtypes per §3.2:38). Bias-AUC (§5.1) and confusion-matrix (§5.2) analyses evaluate **only** the toxicity head. CF analysis (§5.3) evaluates 7 heads but only ensemble-vs-component differences on toxicity head appear in prose (line 167). `identity_attack` head — most directly relevant to identity bias — is silently dropped. **The audit replicates the metric's scope**: the competition metric scores only the toxicity head, so developers had no leaderboard incentive to fairness-tune the other 6 heads, and the audit's silence inherits that scope. This is exactly the system × metric coupling the revised §1 framing claims to make "co-equal." | One sentence in §4.1 or §5.1: "The competition metric scores only the toxicity head; we follow that scope in §5.1 and §5.2 and flag 6 unscored heads as audit-uncovered. The CF analysis (§5.3) does evaluate all 7 heads; identity_attack head bias profiles are deferred to the supplementary cell043 outputs." Add to §6.4 recommendations: "Extend the metric to weight all 7 heads, or accept that subtype-head fairness is untestable on-leaderboard." |
| 5 | major | §6.2 header (line 192) | Heading "Robustness, accuracy, and fairness assessment" still promises analysis the section does not deliver. Technical robustness (adversarial perturbation, distribution shift, training-stochasticity stability, label-noise sensitivity) is not assessed anywhere. The CF analysis touches text-perturbation but lives in §5.3, not in §6.2's summary. | Drop "Robustness" from the heading: "§6.2 Accuracy and fairness assessment." Or add one clause: "Technical robustness (adversarial / distribution shift / training-seed stability) is out of scope; we audit fairness conditional on the deployed model." |
| 6 | major | §6.4:200 (24-column monitoring) + §5.4:181 (9-of-24 cutoff) — **metric-coupled** | §6.3:200 calls for "continuous monitoring of disparate impact across the full twenty-four identity columns, not just the nine," but the audit itself never computes Subgroup AUC + Module-1 metrics on the 15 excluded columns. **The recommendation replicates in the audit's own coverage the metric's blind spot that §5.4 critiques.** This is the cleanest example available of the "system × metric pairing as co-equal objects" framing the new §1 paragraph promises — and currently the audit fails the test it sets for the competition. | Either compute Subgroup AUC + confusion-matrix metrics on the 15 excluded columns (one-row-per-column appendix table with explicit small-$n$ CI caveats), or temper §6.3:200: "expand evaluation to the 15 excluded columns; we did not perform this expansion in this audit, which replicates the metric's coverage gap." |
| 7 | major | §5.4:177 + §5.4:183 (Rawlsian framing inconsistency) | The §5.4 closing sentence has been corrected to "the difference-principle aggregation" (good), but the body of §5.4 line 177 still reads "a narrower reading than Rawlsian fair equality of opportunity, which concerns pre-competition development rather than outcome equalization." Difference principle and FEO are distinct components of Rawls; FEO concerns pre-competition access (lexically prior), the difference principle concerns outcome distribution. p=−5 implements the latter. The body sentence still conflates them while the closing sentence has been corrected — internal inconsistency. | Replace §5.4:177 sentence: "the choice $p = -5$ embeds a difference-principle-style maximin commitment toward the worst-off subgroup. Rawls' separate fair-equality-of-opportunity principle, concerning pre-competition access, is not what the metric encodes." |
| 8 | major | §5 body (vs §1 + §5.4 framing) | **System × metric coupling is now framed at top (§1) and bottom (§5.4 closing) but the inline pairings in §5.1, §5.2, §5.3 are absent.** A reader who buys the §1 framing expects each subsection to close with one sentence connecting its findings to the specific metric-design choice that produced them. Currently §5.1 ends without a metric-coupling sentence; §5.2 ends without one; §5.3 ends without one. The audit promises a paired analysis and delivers two sandwich slices around three system-only paragraphs. | Add one closing sentence per subsection. Examples (one each, total ~3 lines): §5.1 close: "The over/under classification follows from $M_{p=-5}$ over Subgroup AUCs (§5.4); the metric saw and partially compressed these gaps; we quantify the residual." §5.2 close: "FPR/TPR variations are AUC-invariant and therefore off-leaderboard (§5.4); the system was free to be unfair on these axes." §5.3 close: "CF gaps corroborate §5.1 on metric-visible identities; the 9-of-24 cutoff (§5.4) means we cannot assess CF on identities the metric never scored." |
| 9 | major | Tables 1, 2, §4.3:65 | Decimal precision is theatrical. AUC 0.97368 (5 dp), psychiatric Subgroup AUC 0.9560 (4 dp on n=238). For psychiatric n=238, Wald 95% CI half-width on AUC is on the order of 0.02–0.03; trailing digits are noise. Limitation (iv) has been strengthened to "the §6.2 verdict should be read as effect-size pattern, not hypothesis test" — pair this concession with appropriate precision in the tables themselves. | Strip to 3 dp throughout, 2 dp for n<500 subgroups. Score reproduction (0.94464 / 0.94683) can keep 5 dp for the explicit-comparison purpose. |
| 10 | minor | §5.1:106, derived_direction_of_error.md | ±0.005 "balanced" tolerance is arbitrary; never justified or sensitivity-tested. With it, male (+0.0029) and jewish (+0.0023) are balanced; ±0.001 → 0 balanced; ±0.01 → muslim (-0.0065) drops to balanced. Qualitative pattern moves with threshold; the audit's "5/2/2" rests on an undefended choice. | One sentence + one number in §5.1: "At ±0.005, classification is {5 over, 2 under, 2 balanced}; at ±0.001, {6, 3, 0}; at ±0.01, {4, 2, 3}. The four largest-gap over-flag identities are robust; muslim, male, jewish are threshold-sensitive." |
| 11 | minor | §3.1:40 | "11,003 mentions" is sum-with-overlap across 9 columns (a comment mentioning multiple identities is counted in each column), not unique-comment count. Has implications for the $n \geq 238$ floor and limitation (v) on intersectionality. | One clause: "11,003 mentions (sum across columns; comments mentioning multiple identities are counted in each)." |
| 12 | minor | §3.3:48 | Civil Comments uses **distinct** rater pools for toxicity track and identity track. The current §3.3 partial fix ("if rater demographics under-represent any identity, that identity's rater consensus on what counts as toxic is structurally under-counted at the binarization step") covers toxicity-rater concerns but doesn't surface the identity-rater pool. The joint identity × toxicity distribution inherits noise from both. | One clause: "(Civil Comments uses distinct rater pools for the two tracks; identity-mention labels are themselves rater-aggregate fractions binarized at 0.5.)" |
| 13 | minor | §6.5 limitation (v) | Limitation (v) now says "column-wise harm estimates are lower bounds for commenters mentioning more than one identity" — useful framing fix but no quantification. If 5% of identity-mentioning test comments mention ≥2 of the 9 columns, the column-wise analysis covers most cases; if 30%+, conclusions are weak for nearly a third of the relevant population. | Add one number: "X% of identity-mentioning test comments mention ≥2 of the 9 evaluated columns" (one pandas count). |

## Priority list

- **Item 1 — fix the `transgender` → `straight` factual error in §5.3:141.** One-word fix; the report explicitly points the grader at cell039 to verify the list, and cell039 contradicts it. Highest-priority blocker because it is the cleanest falsifiable error in the current draft.
- **Item 2 — surface the TPR-collapse + AUC-blindness pairing in the §6.2 verdict.** Two sentences; the data is in Table 2 and the §1 + §5.4 framing now demands this canonical example of metric-blindness be in the verdict.
- **Item 8 — add one inline-coupling sentence per §5 subsection.** Three sentences total; without these the system × metric "co-equal" framing of §1 sits as advertisement rather than analysis.
- **Item 6 — temper or actually compute the §6.3 24-column recommendation.** The audit's recommendation that the competition do something the audit didn't is the cleanest internal contradiction in the deployment section.
- **Item 4 — name the toxicity-head-only scope and its metric coupling.** One sentence in §4.1 + one sentence in §6.4. The audit's silence on `identity_attack` head bias is itself a metric-shaped silence.

## Recommendation

**Conditional go: items 1 and 2 are pre-submission must-fix, items 4, 6, 8 are strongly-encouraged-fix, the rest are polish.** The revision since `04ddb66` is substantial and addresses the bulk of pass-1+2 findings; the introduced factual error (item 1) is a one-word fix and the TPR-blindness gap (item 2) is a two-sentence fix that closes the §1+§5.4 framing the revision invested in. With items 1 and 2 applied, the report is submission-ready under the 10-page constraint; items 4, 6, 8 would materially strengthen the audit's headline contribution but are not blockers.
